namespace BackendAPI.Models
{
    public enum DocStatus
    {
        Valid,
        Expiring,
        Expired
    }

    public enum DocType
    {
        Normative,
        Matrix,
        TrainingRecord
    }
}
